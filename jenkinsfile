node {
    checkout scm

    docker.withRegistry('https://registry.hub.docker.com', 'Dochub') {

        def customImage = docker.build("evaligm/psspimage2")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}
